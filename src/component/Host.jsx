import React from 'react';

const Host = () => {
    return (
        <div>
            this is host page
            this is host page
            this is host page
            this is host page
        </div>
    );
};

export default Host;